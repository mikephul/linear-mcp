# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP.
    type: object
    required:
      - linearApiKey
    properties:
      linearApiKey:
        type: string
        description: Linear API key with permissions to manage issues, teams, and projects.
  commandFunction:
    # A JS function that produces the CLI command based on the given config to start the MCP on stdio.
    |-
    (config) => ({
      command: 'node',
      args: ['build/index.js'],
      env: { LINEAR_API_KEY: config.linearApiKey }
    })
  exampleConfig:
    linearApiKey: your-linear-api-key
